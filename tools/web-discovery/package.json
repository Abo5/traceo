{
  "name": "traceo-web-discovery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser sidecar for Traceo: renders a target, describes what it is, and runs the page checks against it.",
  "engines": { "node": ">=20" },
  "scripts": {
    "postinstall": "playwright install chromium",
    "check": "node --check discover.mjs && node --check check.mjs && node --check ssrf.mjs"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
